library("raster", lib.loc="/usr/lib/R/site-library")
?raster
rasterOptions()
install.packages("osmar")
install.packages("osmplotr")
?osmplotr
??osmplotr
structures <- c ('highway', 'park', 'grass')
structs <- osm_structures (structures=structures, col_scheme='light')
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map (osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
dat <- make_osm_map (bbox=bbox, structures=structs)
print_osm_map (dat$map)
library(osmplotr)
structures <- c ('highway', 'park', 'grass')
structs <- osm_structures (structures=structures, col_scheme='light')
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map (osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
dat <- make_osm_map (bbox=bbox, structures=structs)
print_osm_map (dat$map)
dat <- make_osm_map (osm_data=london)
dat <- make_osm_map (osm_data=london)
osm_structures(structures = c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree"), col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map (osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
dat <- make_osm_map (bbox=bbox, structures=structs)
print_osm_map (dat$map)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures (structures=structures, c col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map (osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
dat <- make_osm_map (bbox=bbox, structures=structs)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures (structures=structures, c col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map (osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
dat <- make_osm_map (bbox=bbox, structures=structs)
print_osm_map (dat$map)
libarary(osmplotr)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures(structures=structures, c col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map(osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
#bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
#dat <- make_osm_map(bbox=bbox, structures=structs)
#print_osm_map (dat$map)
libarary(osmplotr)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures(structures=structures, col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map(osm_data=london, structures=structs)
libarary(osmplotr)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures(structures=structures, col_scheme = "dark")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
dat <- make_osm_map(osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
#bbox <- get_bbox (c(-0.15,51.5,-0.10,51.52))
## Not run:
#dat <- make_osm_map(bbox=bbox, structures=structs)
print_osm_map (dat$map)
libarary(osmplotr)
structures <- c("building", "amenity", "waterway", "grass",
"natural", "park", "highway", "boundary", "tree")
structs <- osm_structures(structures=structures, col_scheme = "light")
# make_osm_map returns potentially modified list of data using the provided
# 'london' data:
#dat <- make_osm_map(osm_data=london, structures=structs)
# or download data automatically using a defined bounding boox
bbox <- get_bbox (c(-0.351468, 51.38494, 0.148271, 51.672343))
## Not run:
dat <- make_osm_map(bbox=bbox, structures=structs)
getwd()
setwd('/home/user/jupyter/notebooks/gsProject/output')
getwd()
install.packages("knitr")
getwd(
)
unlink('test_cache', recursive = TRUE)
